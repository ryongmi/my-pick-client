# My Pick 클라이언트 환경 설정 파일
# 이 파일을 .env.local로 복사하고 환경에 맞게 값을 수정하세요

# ========================================
# 서버 URL 설정
# ========================================

# My Pick 백엔드 API URL
NEXT_PUBLIC_API_URL=http://localhost:8300

# My Pick 클라이언트 URL (현재 앱)
NEXT_PUBLIC_APP_URL=http://localhost:3300

# ========================================
# 인증 통합 설정 (krgeobuk 생태계)
# ========================================

# Auth Server URL (SSO 직접 리다이렉트용)
NEXT_PUBLIC_AUTH_SERVER_URL=http://localhost:8000

# Auth Server API URL (백엔드 서비스용)
NEXT_PUBLIC_AUTH_API_URL=http://localhost:8000

# Authz Server API URL (권한 관리용)
NEXT_PUBLIC_AUTHZ_API_URL=http://localhost:8100

# MyPick Server API URL (콘텐츠 관리용)  
NEXT_PUBLIC_MYPICK_API_URL=http://localhost:4000

# ========================================
# 외부 API 설정
# ========================================

# YouTube API 키 (YouTube Data API v3에서 발급)
NEXT_PUBLIC_YOUTUBE_API_KEY=your_youtube_api_key_here

# Twitter API 설정 (Twitter Developer Portal에서 발급)
NEXT_PUBLIC_TWITTER_API_KEY=your_twitter_api_key_here
NEXT_PUBLIC_TWITTER_API_SECRET=your_twitter_api_secret_here
NEXT_PUBLIC_TWITTER_BEARER_TOKEN=your_twitter_bearer_token_here

# ========================================
# 환경 설정
# ========================================

# 애플리케이션 환경 (development, production)
NEXT_PUBLIC_ENVIRONMENT=development
NODE_ENV=development

# ========================================
# Next.js 설정
# ========================================

# Next.js 텔레메트리 비활성화 (프라이버시 보호)
NEXT_TELEMETRY_DISABLED=1

# ========================================
# 선택적 설정 (필요시 주석 해제)
# ========================================

# 디버그 모드 활성화 여부 (true/false)
# NEXT_PUBLIC_DEBUG=false

# 로그 레벨 설정 (error, warn, info, debug)
# NEXT_PUBLIC_LOG_LEVEL=info

# ========================================
# 프로덕션 환경 배포시 참고사항
# ========================================
# 실제 운영 환경에서는 다음과 같이 실제 도메인으로 변경하세요:

# 서버 URL 설정
# NEXT_PUBLIC_API_URL=https://api.mypick.yourdomain.com
# NEXT_PUBLIC_APP_URL=https://mypick.yourdomain.com
# NEXT_PUBLIC_ENVIRONMENT=production
# NODE_ENV=production

# 외부 API 프로덕션 설정
# NEXT_PUBLIC_YOUTUBE_API_KEY=your_production_youtube_api_key
# NEXT_PUBLIC_TWITTER_API_KEY=your_production_twitter_api_key
# NEXT_PUBLIC_TWITTER_API_SECRET=your_production_twitter_api_secret
# NEXT_PUBLIC_TWITTER_BEARER_TOKEN=your_production_twitter_bearer_token

# HTTP 클라이언트 프로덕션 설정 (@krgeobuk/http-client)
# ※ 아래 설정들은 기본값이 있어 로컬에서는 생략 가능하지만, 운영에서는 명시적 설정 권장

# 허용된 도메인 목록 (보안 정책용) - 쉼표로 구분
# 기본값: localhost,127.0.0.1,krgeobuk.com → 운영 도메인으로 변경 필요
# ALLOWED_ORIGINS=yourdomain.com,api.mypick.yourdomain.com,mypick.yourdomain.com

# API 요청 타임아웃 시간 (밀리초) - 기본값: 10000ms → 콘텐츠 로딩 시간 고려하여 증가
# NEXT_PUBLIC_API_TIMEOUT=15000

# 레이트 리미터 설정 - 콘텐츠 서비스는 적절한 제한 필요
# 최대 요청 횟수 (기본값: 100) → 콘텐츠 서비스는 200 권장 (많은 콘텐츠 요청 대응)
# NEXT_PUBLIC_RATE_LIMIT_MAX_ATTEMPTS=200
# 시간 윈도우 (기본값: 60000ms)
# NEXT_PUBLIC_RATE_LIMIT_WINDOW_MS=60000

# 보안 설정 (모든 기본값: true) - 운영에서는 반드시 활성화
# CSRF 보호 활성화
# NEXT_PUBLIC_ENABLE_CSRF=true
# 입력값 검증 활성화
# NEXT_PUBLIC_ENABLE_INPUT_VALIDATION=true
# 보안 로깅 활성화
# NEXT_PUBLIC_ENABLE_SECURITY_LOGGING=true
